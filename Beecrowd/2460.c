#include <stdio.h>
#include <stdlib.h>

struct cel {
    int conteudo[1];
    struct cel *seg;
};

typedef struct cel celula;

void insert(*fila_inicial, *fila_final){
    celula *oof;
    oof->fila_inicial;
    while(oof != NULL){
        oof->*seg;
    }
}
 
int main() {
    int 
    celula *fila_inicial, *fila_final;
    
    fila_inicial = malloc(sizeof(celula));
    fila_inicial->seg = NULL;
    
    fila_final = malloc(sizeof(celula));
    fila_final->seg = NULL;
    
    
 
    return 0;
} //Incompleto
